CREATE TABLE    hr.DEPT2(DEPT_ID    NUMBER(2)   PRIMARY KEY,
                        DNAME       VARCHAR2(14),
                        LOC         VARCHAR2(13),
                        CREATE_DATE DATE);

SELECT * FROM   hr.DEPT2;
